---
name: video-editing
description: 下载和拆解用户提供的参考短视频，并以用户自己的素材复刻其核心结构、节奏、字幕和声画效果；用于爆款参考分析、视频剪辑、对照复剪和基于反馈的持续优化。
---

# 视频剪辑与参考复刻

## 工作起点

根据用户当前输入推进：只有参考时先完成拆解；已有用户素材且要求剪辑时继续制作成片。延续已确认的平台、风格和约束，缺少关键素材或不可推断的参数才询问。不要凭空承诺素材下载、转写或导出能力。

在项目内将媒体与案例产物保存到 `local/`：参考放 `local/references/`，用户素材放 `local/source/`，分析与工程放 `local/projects/`，成片放 `local/exports/`。这些路径由 Git 忽略。可公开的去标识化经验只在确有新结论时写入 `references/`，并从本文件链接。

## 1. 获取和检查

- 链接输入：检查现有下载工具，例如 yt-dlp，实际尝试获取可访问版本；记录来源、时间和下载结果。链接失效、登录限制或平台不支持时说明原因，请用户提供本地文件。
- 文件输入：核对能否解码、时长、分辨率、帧率、横竖屏和音轨；可用 ffprobe 检查。不要只看文件名推断内容。
- 检查可用的逐帧查看、播放、转写和剪辑工具；缺少能力时明确哪部分未经核验。仅有字幕不能代替视觉分析，仅有截图不能代替音频检查。

## 2. 拆解参考

实际查看视频和音轨。按有意义的片段建立表格，时间码统一为 `mm:ss.mmm`：

| 起止时间 | 画面与镜头运动 | 口播/屏幕信息 | 切点与字幕 | 音乐/音效 | 结构作用 | 证据与不确定性 |
| --- | --- | --- | --- | --- | --- | --- |

重点标注首屏与开头钩子、冲突或悬念、承诺兑现、信息或情绪峰值、收尾；记录镜头长度、停顿、字幕分组和声画同步方式。结构由实际内容决定，不强套固定模板。

提炼可复用的精华，说明它在哪个时间段、如何起作用，以及用户素材能否实现。没有播放量、留存或完播数据时，只判断内容结构，不声称已验证爆款机制。

## 3. 映射用户素材

建立素材清单，标注每条素材可用的入点、出点、内容、画质和音质问题。按参考顺序生成可执行剪辑表：参考时间段 → 用户源文件与入出点 → 成片时间段 → 裁切/字幕/声音处理 → 差距与替代。

“1:1 复刻精华”应落实为可核对的结构、节奏和声画处理；优先保留用户表达的真实性，不能为了对应时间线截断语义或编造事实。使用用户素材重建效果；参考中需另行取得的音乐、字体和素材，记录依赖或采用可用替代。

## 4. 制作和交付

根据已有环境选择编辑器或 FFmpeg 等工具，保留可复现的脚本或工程、时间线和字幕文件。字幕以实际发声为准，检查断句、错字、同步与遮挡；核对构图、裁切、转场、音乐与人声平衡。

导出参数沿用用户要求；否则根据素材与发布场景选择并记录。导出后检查文件可解码、音视频时长及同步、黑帧/冻结、字幕越界和声音削波，实际回看开头、切点、峰值和结尾。提供成片路径、工程或复现入口、已检查项目和仍存差距。工具缺失导致只能完成部分时，准确报告边界。

## 5. 评分与复剪

统一采用下列 100 分量表，逐项给出分数、证据和扣分理由：

| 维度 | 满分 | 判断依据 |
| --- | ---: | --- |
| 钩子与结构对应 | 20 | 首屏、推进、兑现、收尾与参考核心作用的对应 |
| 节奏与信息密度 | 20 | 镜头长度、切点、停顿及信息可理解性 |
| 画面与镜头处理 | 15 | 构图、裁切、运动和转场效果 |
| 字幕与视觉层次 | 15 | 准确性、断句、同步、可读性 |
| 人声、音乐与音效 | 15 | 清晰度、平衡、同步和情绪配合 |
| 素材适配与表达完整 | 10 | 用户内容真实、语义完整、替代合理 |
| 技术交付质量 | 5 | 可播放、导出正确、可复现 |

未能观察的维度标“未核验”，不凭空给分或把其余项目折算成满分。每轮先解决影响最大的差距，再对照同一参考复查。用户认可与实际数据反馈分别记录；模型自评 90 分不能替代用户验收，也不能保证传播效果。

把真实案例中验证有效的改动沉淀为规则或可复用脚本，并记录适用条件与反例。缺少参考和用户素材时，将 skill 标记为初版、尚未通过实片验证。
