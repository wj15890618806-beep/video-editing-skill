# 视频剪辑 Skill 项目规则

## 项目目标

持续打磨视频剪辑 skill：用户提供参考视频链接或文件后，下载并分析爆款结构；用户提供自己的视频后，将参考中的核心表达和剪辑精华映射到用户素材，尽可能实现 1:1 的结构、节奏、字幕、画面和声音效果。通过真实案例、用户反馈和复剪，持续向 90/100 分的水准迭代。

## Git 与 GitHub：强制执行

- 用户已授权连接其 GitHub 账号，并为本项目创建 public 仓库。
- 之后所有项目改动，每轮完成必要检查后，都必须本地 `git commit`，并向对应远程分支 `git push`；不得只修改文件而省略提交或推送。
- 提交前检查 diff 和待提交文件，用清晰的提交信息说明改动。避免覆盖用户改动，不默认强推、重写历史或删除远程内容。
- 最终交付注明验证结果、commit 和 push 状态；只有远程同步已核实，才能声称已推送。
- 若身份验证、网络或权限阻止操作，保留本地成果，明确失败原因和待完成动作；不得虚报已完成，也不得索取或将 token 写入仓库。
- 本仓库公开。原始参考视频、用户视频、音频、截图、逐字稿、剪辑工程、导出成片及带私人信息的案例默认只保存在被 Git 忽略的本地目录。提交可复用的方法、脚本和去标识化复盘。不得把“所有改动 push”解释为公开私人素材或凭证。

## 视频处理与持续打磨

- 执行视频任务前读取根目录 `SKILL.md`。
- 初剪使用已内化的 video-use 流程，读取 `references/video-use.md`，通过 `scripts/video_use.py` 调用仓库内固定版本的脚本。上游技能的默认目录、英文字幕风格及重复确认流程按本项目约定适配。
- 收到链接时先实际尝试下载可访问的视频，保存来源、获取时间和本地文件路径；无法下载时如实说明具体限制，再请用户提供文件。不得假装已观看或绕过访问控制。
- 根据实际视频和音轨提供带时间码的分析：开头钩子、内容推进、信息密度、镜头切点、字幕、音效、音乐、情绪变化和结尾行动引导。区分观察、推测和已有数据，不把主观判断说成爆款因果。
- 对用户素材先建立素材清单，再制定参考与素材逐段对应的剪辑表；优先复刻最影响观看体验的部分。素材不足时明确差距并给出可执行替代或补拍建议。
- 用户要求剪辑时必须执行可行的剪辑与导出，并检查成片；仅有分析或方案不等于完成剪辑。
- 每轮记录用户反馈、问题、修改和复查结果，只把经过案例支持的经验沉淀为 skill 规则；不要因单个案例无限堆叠通用要求。
- 按 `SKILL.md` 的评分维度解释分数和依据。90 分是迭代目标，不是对流量的保证；未检查成片或未收到用户反馈时应注明暂评，不能宣称已达标。
